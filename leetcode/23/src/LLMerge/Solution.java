package LLMerge;

public class Solution {

    private Node mergeLists(Node list1, Node list2){
	/*
	int i = 0, j = 0;
	Node[] newList = new Node[list1.length + list2.length];
	while(i < list1.len && j < list2.len) {

	    if(list1 <= list2[j]){

		newList[i + j] = list1[i];
		i++;

	    } else {
		
		newList[i + j] = list2[i];
		j++;

	    }

	}

	return newList;
	*/
	Node n = new Node(-1);
	return n;
    }
    
    public Node mergeKLists(Node[] lists) {
	/*
	Node[] largeList = ;
	for(int i = 0; i < lists.size(); i++){
	    largeList = mergeLists(largeList, lists[i]);
	}
	return largeList;
	*/
	Node n = new Node(-1);
	return n;
    }
}
